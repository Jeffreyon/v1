function Pill({ label }) {
    return (
        <span className="px-3 bg-amber-100 text-amber-800 border border-amber-200 rounded-full font-mono">
            {label}
        </span>
    );
}

export default Pill;
